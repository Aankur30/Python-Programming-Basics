# is used as a comment in the python
new_var="this is a string"
print(new_var);
new_var =["this is a string in the list",500,30.075];
print(new_var);
# print provide line break itself only when the next line is executed
# this shows that the python variables are dynamically typed
print(type(new_var))
